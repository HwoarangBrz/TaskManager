import { TaskClosedDirective } from './task-closed.directive';

describe('TaskClosedDirective', () => {
  /* it('should create an instance', () => {
    const directive = new TaskClosedDirective();
    expect(directive).toBeTruthy();
  }); */
});
